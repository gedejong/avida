# Rust Migration Waves

This roadmap keeps the codebase in mixed C/C++ + Rust mode while incrementally migrating modules behind stable C ABI boundaries, and records forward-looking cleanup in `documentation/Tech-Debt-Backlog.md`.

Program-wide mandatory slice gates are defined in `rust-migration-execution-playbook.md` and remain unchanged.

## Wave 1: Low-coupling utilities

- Completed Wave 1 module entries archived in `documentation/archive/rust-migration-waves-completed.md`.

Exit criteria:
- Build and test gates pass (`./build_avida -DAVD_UNIT_TESTS:BOOL=ON`, `./run_tests --mode=slave`, `./cbuild/work/unit-tests`).
- Rust crate quality gates pass (`cargo test`, `cargo fmt --check`, `cargo clippy -- -D warnings` in `rust/avida-rust`).
- Rust coverage gate passes (`./scripts/ci_coverage_check.sh 75` in `rust/avida-rust`; CI `coverage-rust` lane), with initial scope focused on actively migrated FFI-centric modules.
- FFI invariants are documented and followed: opaque handles, explicit create/free, null-safe no-op reads/writes, and no cross-language frees.

## Wave 2: Deterministic weighted selectors

- Completed Wave 2 module entries archived in `documentation/archive/rust-migration-waves-completed.md`.

Focus:
- Preserve exact floating-point behavior for weighted lookups.
- Keep C++ wrappers for existing Apto container interactions.

## Wave 3: Data utilities with side effects isolated

- Completed Wave 3 module entries archived in `documentation/archive/rust-migration-waves-completed.md`.

Focus:
- Replace direct file side effects with explicit API hooks where possible.
- Add edge-case tests for bit operations and histogram boundaries.

## Wave 4: Medium-coupling analysis/data modules

- Selected modules in `source/analyze`
- Selected modules in `source/data`
- `Data::Package` (Rust-backed deterministic `ArrayPackage` conversion/format, `Wrap<Apto::String>` parse helpers, and primitive `Wrap<bool/int/double>::StringValue` formatting via C ABI)
- `Data::TimeSeriesRecorder` (Rust-authoritative state for parse/serialize + append + typed reads via C ABI; C++ mirror state removed)
- `Data::Provider` deterministic helper dispatch/parse path (Rust-backed C ABI helpers; classify/split dispatch centralized through `avd_provider_classify_id` with expanded edge-shape matrix coverage and consistency fixture lock)
- `Data::Manager` deterministic argumented-ID classify/split path (Rust-backed C ABI helpers reused across active/describe/attach/get/register flows, including provider registration/activation mapping and edge-shape parity fixtures)

Focus:
- Define narrow ABI seams around pure computation and deterministic transforms.
- Avoid crossing ownership boundaries with complex object graphs.
- Keep `rust/avida-rust/src/lib.rs` as a thin hub with per-domain module ownership.
- Evaluate mature external crates first for each slice; keep custom code only when parity/ABI constraints require it.
- Reuse shared Rust helper parsers across `source/data` modules to eliminate duplicated C++ bracket parsing logic.
- Consolidate duplicated FFI CString/output-pointer handling through shared `common.rs` helpers (`provider_helpers`, `time_series_recorder`, `package`) to reduce repeated unsafe patterns.
- Idiomatic Rust FFI hardening (completed): centralized handle lifecycle helpers in `common.rs`, normalized out-parameter write handling in selected FFI modules, and added CI artifacts for exported symbol map + LLVM call graph with an ABI removal guard.
- Idiomatic Rust FFI hardening phase 2 (completed): removed remaining `not_unsafe_ptr_arg_deref` allowances from `bit_array`, `time_series_recorder`, and `resource_count_helpers`; unified legacy bool/int/double coercion internals in `common.rs` for shared use by package/time-series helpers; added an FFI out-param policy note; and extended CI FFI artifacts with compact `ffi_summary.txt`.

## Wave 5: Runtime and execution core

- Selected modules in `source/main`
- Selected modules in `source/cpu`
- `cResourceCount` deterministic helper paths (Rust-backed FFI lookup, inflow/decay precalc helper math, and update-step scheduling helper math used by `GetResourceCountID`/`GetResourceByName`/`SetInflow`/`SetDecay`/`Update`/`DoUpdates` step derivation)
- `cResourceCount` setter-precalc table-fill helpers (completed): Rust-backed additive helpers now populate `SetInflow`/`SetDecay` recurrence tables per resource row while C++ retains setter sequencing, ownership, and lookup/error behavior.
- `cSpatialResCount::FlowAll` per-neighbor transfer accumulation (completed): Rust-backed additive pair-delta helper now computes neighbor transfer deltas used by `FlowAll` while C++ retains grid traversal, neighbor lookup, and state mutation ordering.
- `cSpatialResCount` aggregate-update helpers (completed): Rust-backed additive `StateAll` fold and `SumAll` reduction helpers now compute deterministic per-cell fold/reduction math while C++ retains container ownership and call sequencing.
- `cSpatialResCount` bulk-rate/reset helpers (completed): Rust-backed additive helpers now compute deterministic per-cell `RateAll` delta progression and `ResetResourceCounts` amount reset math while C++ retains traversal order, ownership, and call sequencing.
- `cSpatialResCount` SetCellList cell-init helpers (completed): Rust-backed additive helper now computes deterministic per-cell initialization fold used by `SetCellList` while C++ retains cell-list traversal, bounds policy, and state ownership/order.
- `cResourceCount` spatial dispatch helpers (completed): Rust-backed additive helpers now compute spatial step iteration and cell-list branch policy used by `DoSpatialUpdates` while C++ retains update traversal and operation order (`UpdateCount/Source/Sink/CellInflow/CellOutflow/FlowAll/StateAll`).
- `cResourceCount` per-resource dispatch policy helpers (completed): Rust-backed additive helpers now compute geometry-based spatial classification plus per-resource `DoUpdates` action selection and `m_last_updated` advance policy, while C++ retains resource iteration order, update scheduling, and state ownership/mutation sequencing.
- `cResourceCount` update-time accumulation and wrapper-policy helpers (completed): Rust-backed additive helpers now define update-time delta passthrough and wrapper `global_only` mode mapping used by `Update`/`UpdateGlobalResources`/`UpdateRandomResources`/`UpdateResources`, while C++ retains call sequencing and state ownership.
- `cResourceCount` read-path policy wiring (completed): C++ read entrypoints now route geometry-based global-vs-spatial selection through Rust helper `avd_rc_read_path_kind` (`ReadCellResourceValue` and aggregate `Get` path), preserving ownership and `DoUpdates` ordering with refreshed C++ parity checks for mapping and payload selection.
- `cResourceCount` write-path policy wiring (completed): C++ write entrypoints now route geometry-based global-vs-spatial selection through Rust helper `avd_rc_is_spatial_geometry` in `Set`/`ModifyCell`, preserving mutation ordering, `SetModified` side effects, and ownership with refreshed C++ policy-matrix parity checks.
- `cResourceCount` `SetCellResources` write-path policy wiring (completed): C++ `SetCellResources` now routes geometry-based global/partial no-op vs spatial write selection through Rust helper `avd_rc_setcell_write_path_kind`, preserving traversal order, ownership, and intentional global no-op semantics with refreshed Rust and C++ policy-matrix parity checks.
- `cResourceCount` spatial setup/resize policy wiring (completed): C++ `Setup` and `ResizeSpatialGrids` now route geometry bucket classification, spatial rectangle logging policy, and resize cell-count derivation through Rust helpers (`avd_rc_setup_path_kind`, `avd_rc_should_log_spatial_rectangles`, `avd_rc_resize_cell_count`) while preserving allocation ownership, traversal order, and legacy branch semantics.
- `cResourceCount` `SetGradientCount` setter-sequence policy wiring (completed): C++ `SetGradientCount` now routes deterministic gradient setter ordering through Rust helpers (`avd_rc_gradient_setter_count`, `avd_rc_gradient_setter_opcode`) while preserving C++ object ownership, per-setter mutation calls, and final `ResetGradRes(ctx, worldx, worldy)` ordering with refreshed Rust/C++ sequence parity checks.
- `cResourceCount` gradient scalar setter policy wiring (completed): C++ scalar gradient setter wrappers now route deterministic setter opcode selection through Rust helpers (`avd_rc_gradient_scalar_setter_count`, `avd_rc_gradient_scalar_setter_opcode`) while preserving C++ assertions, direct setter side effects, and ownership semantics with refreshed Rust/C++ parity and guard checks.
- `cResourceCount` gradient variable inflow setter policy wiring (completed): C++ `SetGradPlatVarInflow` routes deterministic var-inflow setter opcode selection through Rust helpers (`avd_rc_gradient_var_inflow_setter_count`, `avd_rc_gradient_var_inflow_setter_opcode`) while preserving C++ assertions, context threading, direct setter side effects, and ownership semantics with Rust/C++ parity and guard checks.
- `cResourceCount` predatory resource setter policy wiring (completed): C++ `SetPredatoryResource` now routes deterministic setter opcode selection through Rust helpers (`avd_rc_predatory_setter_count`, `avd_rc_predatory_setter_opcode`) while preserving direct setter side effects (`odds`, `juvsper`) and C++ ownership semantics with Rust/C++ sequence and guard parity coverage.
- `cResourceCount` probabilistic resource setter policy wiring (completed): C++ `SetProbabilisticResource` now routes deterministic setter opcode selection through Rust helpers (`avd_rc_probabilistic_setter_count`, `avd_rc_probabilistic_setter_opcode`) while preserving context threading, direct setter payload side effects (`initial`, `inflow`, `outflow`, `lambda`, `theta`, `x`, `y`, `count`), and ownership semantics with Rust/C++ sequence and guard parity coverage.
- `cResourceCount` peak getter policy consolidation (completed): C++ `GetCurrPeakX`/`GetCurrPeakY`/`GetFrozenPeakX`/`GetFrozenPeakY` now route deterministic getter opcode selection and update-requirement policy through Rust helpers (`avd_rc_peak_getter_count`, `avd_rc_peak_getter_opcode`, `avd_rc_peak_getter_requires_update`) while preserving current-vs-frozen update semantics and ownership.
- `cResourceCount` shared setter/getter matrix-test consolidation (completed): Rust and C++ parity harnesses now run a unified opcode-family matrix over gradient, scalar, var-inflow, predatory, probabilistic, and peak-getter families (count/order/guard/payload/update-policy checks) to lock policy consistency and reduce per-family drift risk without changing runtime behavior.
- Wave 7 starter seam (completed): `cTaskLib::FractionalReward` now routes deterministic bit-similarity reward scoring through additive Rust helper `avd_tasklib_fractional_reward_bits`, with Rust/C++ parity tests locking identity, one-bit/full-diff, symmetry, and mixed-bit matrix cases.
- Wave 7 registration-family starter seam (completed): `cTaskLib::AddTask` now routes deterministic registration-family gating for the large `logic_3` + `math_1` task-name chain through additive Rust helper `avd_tasklib_is_logic3_or_math1_name`, preserving C++ task wiring/ownership semantics with Rust/C++ classifier parity checks.
- Wave 7 registration-family follow-on seam (completed): `cTaskLib::AddTask` now routes deterministic registration-family gating for `math_2` + `math_3` chains through additive Rust helper `avd_tasklib_is_math2_or_math3_name`, preserving C++ task wiring/ownership semantics with Rust/C++ classifier parity checks.
- Wave 7 registration-family follow-on seam (completed): `cTaskLib::AddTask` now routes deterministic registration-family gating for fibonacci individual names, matching/sequence names, and load-based helper chains through additive Rust helpers (`avd_tasklib_is_fibonacci_name`, `avd_tasklib_is_matching_sequence_name`, `avd_tasklib_is_load_based_name`), preserving C++ ownership/task wiring semantics with refreshed Rust/C++ classifier parity checks and full gate validation.
- Wave 7 scoring follow-on seam (completed): `cTaskLib` threshold/halflife quality-decay scoring policy now routes through additive Rust helper `avd_tasklib_threshold_halflife_quality` across `Task_MatchNumber`, `Task_Mult`, `Task_Div`, `Task_Log`, `Task_Log2`, `Task_Log10`, `Task_Sqrt`, `Task_Sine`, and `Task_Cosine`, preserving C++ ownership/call sequencing with Rust/C++ parity coverage for threshold, sign-normalization, and zero-halflife edge behavior.
- Wave 7 scoring follow-on seam (completed): `cTaskLib` unary math per-input diff policy now routes through additive Rust helper `avd_tasklib_unary_math_input_diff` with explicit opcodes (`AVD_TASKLIB_UNARY_OP_LOG/LOG2/LOG10/SQRT/SINE/COSINE`) across `Task_Log`, `Task_Log2`, `Task_Log10`, `Task_Sqrt`, `Task_Sine`, and `Task_Cosine`, preserving C++ input traversal/min-selection ordering and ownership with Rust/C++ opcode and invalid-guard parity coverage.
- Wave 7 scoring follow-on seam (completed): `cTaskLib` binary pair diff policy now routes through additive Rust helper `avd_tasklib_binary_pair_input_diff` with explicit opcodes (`AVD_TASKLIB_BINARY_OP_MULT`/`AVD_TASKLIB_BINARY_OP_DIV`) across `Task_Mult` and `Task_Div`, preserving C++ pair traversal/min-selection ordering and divide-by-zero skip semantics with Rust/C++ opcode and guard parity coverage.
- Wave 7 scoring follow-on seam (completed): `cTaskLib` shared diff-scan reducer policy now routes through additive Rust helpers (`avd_tasklib_diff_scan_init`, `avd_tasklib_diff_scan_update`) across unary/binary scoring loops (`Task_Mult`/`Task_Div`/`Task_Log`/`Task_Log2`/`Task_Log10`/`Task_Sqrt`/`Task_Sine`/`Task_Cosine`), preserving C++ traversal order and ownership while centralizing accumulator init/update semantics with Rust/C++ parity checks.
- Wave 8 analyze pilot seam (completed): `cAnalyze::CommandFilter` relation-token classification now routes through additive Rust helper `avd_analyze_relation_mask`, preserving C++ parse flow, batch filtering ownership, and `CompareFlexStat` ordering semantics while replacing local relation-token if/else chains with a deterministic Rust policy mask and Rust/C++ parity guard coverage.
- Wave 8 analyze follow-on seam (completed): `cAnalyze` output file-type extension token classification now routes through additive Rust helper `avd_analyze_is_html_extension` across detail/histogram output commands, preserving C++ filename parsing flow and formatter ownership semantics while replacing repeated local `"html"` checks with deterministic Rust policy classification plus Rust/C++ parity guard coverage.
- Wave 8 analyze follow-on seam (completed): `cAnalyze` text/html argument-token file-type selection policy now routes through additive Rust helper `avd_analyze_apply_file_type_token_policy` at `arg_list.PopString("text"/"html")` analyze output call-sites, preserving C++ argument parsing flow and formatter ownership semantics while centralizing deterministic token-precedence behavior (`html` overrides `text`) with Rust/C++ parity coverage.
- Wave 8 analyze follow-on seam (completed): `cAnalyze` filename-token html classification now routes through additive Rust helper `avd_analyze_is_html_filename_token` at remaining `filename == "html"` selection call-sites (`CommandDetailIndex`, `AnalyzeInstructions`), preserving C++ output generation ownership/order semantics with Rust/C++ parity guard coverage.
- Wave 8 action pilot seam (completed): `PopulationActions` deterministic deme-injection dispatch policy now routes through additive Rust helpers (`avd_popaction_deme_loop_start_index`, `avd_popaction_seed_deme_action`) across `InjectDemes`, `InjectModuloDemes`, `InjectDemesFromNest`, and `InjectDemesRandom`, preserving C++ injection ownership/order and existing per-deme mutation semantics with Rust/C++ parity guard coverage.
- Wave 8 action follow-on seam (completed): `PopulationActions` cell-range normalization policy now routes through additive Rust helper `avd_popaction_normalize_cell_end` across injection/parasite constructor ranges (`InjectRange`, `InjectSequence`, `InjectSequenceWithDivMutRate`, `InjectParasite`, `InjectParasiteSequence`, `InjectParasitePair`, `ClearParasites`), preserving C++ range semantics and ownership flow with Rust/C++ parity guard coverage.
- Wave 8 action follow-on seam (completed): `PopulationActions` cell-range validity policy now routes through additive Rust helpers (`avd_popaction_is_valid_cell_range`, `avd_popaction_is_valid_cell_range_with_stride`) across range-checked actions (`InjectRange`, `InjectSequence`, `InjectSequenceWithDivMutRate`, `InjectParasite`, `InjectParasiteSequence`, `InjectParasitePair`, `ClearParasites`), preserving C++ warning/flow ownership semantics while centralizing deterministic invalid-range guards with Rust/C++ parity coverage.
- Wave 8 action follow-on seam (completed): `PopulationActions` filename-token requiredness policy for file-backed injectors now routes through additive Rust helper `avd_popaction_is_missing_filename_token` across `Inject`, `InjectWithInstSet`, `InjectRange`, `InjectWellMixed`, `InjectGroup`, `InjectDemes`, and `InjectModuloDemes`, preserving C++ warning/abort behavior, ownership flow, and orchestration order with Rust/C++ parity guard coverage.
- Wave 8 action follow-on seam (completed): `PopulationActions` `InjectWellMixed` cell-count validity policy now routes through additive Rust helper `avd_popaction_is_valid_well_mixed_cell_count`, preserving C++ warning behavior (`InjectWellMixed has invalid range!`), ownership flow, and orchestration order while centralizing deterministic bounds checks (`0 <= cell_count <= population_size`) with Rust/C++ parity coverage.
- Wave 8 action follow-on seam (completed): `PopulationActions` `InjectGroup` cell-id bounds policy now routes through additive Rust helper `avd_popaction_is_valid_group_cell_id`, preserving C++ ownership flow and orchestration order while preventing out-of-bounds cell access through explicit deterministic guard routing (`0 <= cell_id < population_size`) and warning behavior (`InjectGroup has invalid cell id!`) with Rust/C++ parity coverage.
- Wave 8 action follow-on seam (completed): `PopulationActions` single-cell id bounds policy now routes through additive Rust helper `avd_popaction_is_valid_single_cell_id` at `Inject` and `InjectRandom` call-sites, preserving existing filename/error handling flow while centralizing deterministic cell-id bounds checks (`0 <= cell_id < population_size`) and explicit invalid-input warning behavior (`Inject has invalid cell id!`, `InjectRandom has invalid cell id!`) with Rust/C++ parity coverage.
- Wave 8 action follow-on seam (completed): `PopulationActions` `InjectGroup` now consolidates onto shared single-cell id helper routing (`avd_popaction_is_valid_single_cell_id`) while retaining additive `avd_popaction_is_valid_group_cell_id` ABI surface, preserving warning text (`InjectGroup has invalid cell id!`) plus ownership/orchestration semantics with parity coverage asserting group-vs-single-cell helper equivalence.
- Wave 8 action follow-on seam (completed): `PopulationActions` `InjectParasite` only-if-extinct early-return decision now routes through additive Rust helper `avd_popaction_should_skip_parasite_injection`, preserving C++ orchestration order and early-return behavior while centralizing deterministic skip policy (`only_if_parasites_extinct != 0 && num_parasites != 0`) with Rust/C++ parity guard coverage.
- Wave 8 action follow-on seam (completed): `PopulationActions` parasite filename-token requiredness policy now routes through additive Rust helpers (`avd_popaction_is_missing_parasite_filename_token`, `avd_popaction_has_missing_parasite_pair_filenames`) across file-backed parasite injectors (`InjectParasite`, `InjectParasitePair`), preserving existing error text (`error: no organism file specified`) and orchestration order while centralizing deterministic filename-missing guards with Rust/C++ parity coverage.
- Wave 8 action follow-on seam (completed): `PopulationActions` parasite required-label-token validation now routes through additive Rust helper `avd_popaction_is_missing_parasite_label_token` across parasite injectors (`InjectParasite`, `InjectParasiteSequence`, `InjectParasitePair`), preserving orchestration order while centralizing deterministic missing-label guards and emitting explicit user feedback (`error: no parasite label specified`) with Rust/C++ parity coverage.
- Wave 8 action follow-on seam (completed): `PopulationActions` `InjectParasiteSequence` sequence-token requiredness now routes through additive Rust helper `avd_popaction_is_missing_parasite_sequence_token`, preserving orchestration order and centralizing deterministic missing-sequence guards while emitting explicit user feedback (`error: no parasite sequence specified`) with Rust/C++ parity coverage.
- Wave 8 action follow-on seam (completed): `PopulationActions` parasite invalid-range warning-message selection now routes through additive Rust helper `avd_popaction_parasite_invalid_range_warning_kind` across `InjectParasite`, `InjectParasiteSequence`, and `InjectParasitePair`, preserving existing warning strings while centralizing deterministic action-to-warning-kind policy mapping with Rust/C++ parity coverage.
- Wave 8 action follow-on seam (completed): `PopulationActions` parasite missing-token error-message selection now routes through additive Rust policy selection for missing-token branches across `InjectParasite`, `InjectParasiteSequence`, and `InjectParasitePair`, preserving existing error strings (`error: no organism file specified`, `error: no parasite label specified`, `error: no parasite sequence specified`) and orchestration order with Rust/C++ parity coverage.
- Wave 8 action follow-on seam (completed): `PopulationActions` parasite missing-token short-circuit branch ordering now routes through additive Rust helper `avd_popaction_parasite_missing_token_short_circuit_kind`, consolidating per-action first-missing-token precedence (`InjectParasite`: filename->label, `InjectParasiteSequence`: sequence->label, `InjectParasitePair`: pair-filename->label) while preserving existing error strings and orchestration order with Rust/C++ parity coverage.
- Wave 8 action follow-on seam (completed): `PopulationActions` parasite invalid-range warning emission short-circuit now routes through additive Rust helper `avd_popaction_parasite_warning_short_circuit_kind`, centralizing invalid-range gating plus action-to-warning-kind selection for `InjectParasite`, `InjectParasiteSequence`, and `InjectParasitePair` while preserving existing warning strings and orchestration order with Rust/C++ parity coverage.
- Wave 8 analyze follow-on seam (completed): `cAnalyze` file-type token precedence short-circuit now routes through additive Rust helper `avd_analyze_file_type_token_short_circuit_kind` at `CommandMapTasks` and `CommandMapMutations`, centralizing deterministic token precedence (`html` overrides `text`; no token preserves default) while preserving parser ordering and text/html behavior with Rust/C++ parity coverage.
- Wave 8 print-action pilot seam (completed): `PrintActions` deterministic instruction filename-mode selection policy now routes through additive Rust helper `avd_printaction_instruction_filename_mode` for `PrintInstructionData` and `PrintFromMessageInstructionData`, preserving C++ output ownership/order and file naming semantics (`default plain` vs `keep provided` vs `format with inst_set`) with Rust/C++ parity guard coverage.
- Wave 7 population pilot seam (completed): `cPopulation` deterministic deme-routing policy now routes through additive Rust helpers (`avd_cpop_should_check_implicit_deme_repro`, `avd_cpop_should_run_speculative_deme_block`) in `ProcessStep` and `ProcessStepSpeculative`, preserving C++ update/repro ownership/order semantics with Rust/C++ parity guard coverage.
- Wave 8 print-action follow-on seam (completed): `PrintActions` instruction-data filename-mode selection policy expansion now routes through `avd_printaction_instruction_filename_mode` across remaining instruction-data action constructors (`PrintPreyInstructionData`, `PrintPredatorInstructionData`, `PrintTopPredatorInstructionData`, `PrintPreyFromSensorInstructionData`, `PrintPredatorFromSensorInstructionData`, `PrintTopPredatorFromSensorInstructionData`, `PrintGroupAttackData`), preserving C++ output ownership/order and naming semantics with parity guard coverage.
- Wave 8 print-action follow-on seam (completed): `PrintActions` instruction output sink-selection short-circuit now routes through additive Rust helper `avd_printaction_instruction_output_sink_kind` across instruction-data print actions (`PrintInstructionData`, `PrintFromMessageInstructionData`, `PrintPreyInstructionData`, `PrintPredatorInstructionData`, `PrintTopPredatorInstructionData`, `PrintPreyFromSensorInstructionData`, `PrintPredatorFromSensorInstructionData`, `PrintTopPredatorFromSensorInstructionData`, `PrintGroupAttackData`), preserving output ownership/order and filename-mode behavior with Rust/C++ parity coverage.
- Wave 8 population follow-on seam (completed): `cPopulation` deme-routing short-circuit now routes through additive Rust helper `avd_cpop_deme_routing_short_circuit_kind` in `ProcessStep` and `ProcessStepSpeculative`, centralizing routing-mode-aware block decisions (`process-step`: run when `num_demes >= 1`; `speculative-step`: run when `num_demes > 1`) while preserving update/reproduction ordering semantics with Rust/C++ parity coverage.
- Wave 8 analyze follow-on seam (completed): `cAnalyze` output file-type resolution short-circuit now routes through additive Rust helper `avd_analyze_output_file_type_short_circuit_kind` at extension-driven output sites, centralizing deterministic resolution (`html` extension selects HTML; otherwise keep current default) while preserving parser ordering and text/html behavior with Rust/C++ parity coverage.
- Wave 8 analyze follow-on seam (completed): `cAnalyze` output sink-selection short-circuit now routes through additive Rust helper `avd_analyze_output_sink_short_circuit_kind` at output dispatch sites, centralizing deterministic sink routing (`cout` filename token selects stdout sink; otherwise file sink) while preserving parser ordering and output behavior with Rust/C++ parity coverage.
- Wave 8 population follow-on seam (completed): `cPopulation` deme counter-update short-circuit now routes through additive Rust helper `avd_cpop_should_update_deme_counters` at organism birth/death/birth-count update sites, centralizing deterministic `num_demes > 0` gating while preserving update/reproduction ordering behavior with Rust/C++ parity coverage.
- Wave 8 analyze follow-on seam (completed): `cAnalyze` output file-handle mode short-circuit now routes through additive Rust helper `avd_analyze_output_file_handle_mode_short_circuit_kind` at detail/detail-timeline/histogram file output sites, centralizing deterministic file-handle selection (`CreateWithPath` for detail, `StaticWithPath` for timeline/histogram) while preserving parser ordering and output behavior with Rust/C++ parity coverage.
- Wave 8 population follow-on seam (completed): `cPopulation` multi-deme block short-circuit now routes through additive Rust helper `avd_cpop_should_run_multi_deme_block` at `CheckImplicitDemeRepro` and `UpdateDemeStats` early-return gates, centralizing deterministic `num_demes > 1` routing while preserving update/reproduction ordering behavior with Rust/C++ parity coverage.
- Wave 8 population follow-on seam (completed): `cPopulation` speculative-step multi-deme routing now aligns with shared `avd_cpop_should_run_multi_deme_block` policy in `ProcessStepSpeculative`, removing duplicated `num_demes > 1` branch semantics while preserving update/reproduction ordering behavior with Rust/C++ parity coverage.
- Wave 8 analyze follow-on seam (completed): `cAnalyze` output token-presence short-circuit now routes through additive Rust helper `avd_analyze_output_token_presence_short_circuit_kind` at output filename/time token parsing sites (`PrintTasks`, `PrintTasksQuality`, `Detail`, `DetailTimeline`, `Histogram`), centralizing deterministic optional-token gating while preserving parser ordering and output behavior with Rust/C++ parity coverage.
- Wave 6 CPU pilot seam (completed): `cHardwareCPU::SingleProcess_ExecuteInst` now routes deterministic pre-dispatch instruction-family classification and counted-opcode policy through additive Rust helpers (`avd_cpu_dispatch_family`, `avd_cpu_dispatch_counted_opcode`) while preserving C++ instruction dispatch ownership/order and function-table execution semantics, with Rust/C++ parity coverage and benchmark expansion (`cpu_dispatch_helpers/dispatch_family_and_counted_opcode`).
- Completed Wave 5 slice history archived in `documentation/archive/rust-migration-waves-completed.md`.
- Starter seam definition remains in `documentation/Wave5-cResourceCount-Starter-Seam.md` for follow-on expansion

Focus:
- Migrate only after FFI and release-process maturity from waves 1-4.
- Introduce migration slices that can be toggled independently in CI.
- Wave 5 resource runtime extraction: **COMPLETE** — 42 C++ call-sites, 35+ FFI exports, resource runtime now orchestration-only.
- Waves 6–8 executing in parallel by seam-readiness (not sequentially).
- Wave 8 action/analyze extraction approaching saturation (`PopulationActions` 45 calls, `cAnalyze` 26 calls, `PrintActions` 18 calls).
- Next candidate: extract deterministic `cEnvironment.cc` reaction-process-type dispatch classifiers (`SetType` string→enum, `SetPhenoPlasticBonusMethod` string→enum, entry-type classification) behind additive Rust FFI while preserving C++ configuration parsing flow and ownership semantics.
